<nav class="navbar">
    <div class="container">
        <ul class="position-left">
            <li><%= link_to 'Home', root_url %></li>
            <li><%= link_to 'Book', book_index_url %></li>
            <li><%= link_to 'Author', author_index_url %></li>
            <% if user_signed_in? && current_user.role == 'admin' %>
                <li><%= link_to 'User', user_index_url %></li>
            <% end %>
        </ul>
        <ul class="position-right">
            <% if user_signed_in? %>
                <li> <p style="color: white"><%= current_user.email %></p> <%= button_to "logout", destroy_user_session_path, method: :delete, class: "remove-button navbar-button"  %></li>
            <% else %>
                <li><%= link_to 'Login', new_user_session_url %></li>
                <li><%= link_to 'Register', new_user_registration_url %></li>
            <% end %>
        </ul>
    </div>
</nav>